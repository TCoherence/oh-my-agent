"""Versioned JSON APIs for the dashboard.

``v1`` is the only present version. Versioning is in the URL prefix
(``/api/v1/...``) so breaking changes can ship a parallel v2 without
disrupting frontend code pinned to v1.
"""
